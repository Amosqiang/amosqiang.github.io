name: Sync Blog Posts to X

permissions:
  contents: write

on:
  # 每次静态站有新 commit（也就是私库部署完推过来）就跑
  push:
    branches: [ main ]
    paths-ignore:
      - 'last_post_guid.txt'
  # 也可以在 Actions 页面里手动点运行
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest

    env:
      PYTHON_VERSION: '3.11'
      STATE_FILE_PATH: './last_post_guid.txt'
      # 如果这个地址 404，可以改成 https://amosqiang.github.io/feed.xml
      RSS_FEED_URL: 'https://amosqiang.github.io/index.xml'
      COMMIT_USER_NAME: 'github-actions[bot]'
      COMMIT_USER_EMAIL: 'github-actions[bot]@users.noreply.github.com'
      COMMIT_MESSAGE: 'chore: update last tweeted post GUID'
      REQUIRED_TAG: "X"
      TWEET_FORMAT: "{title} {link}"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser tweepy

      - name: Wait for feed propagation (optional)
        run: sleep 30s

      - name: Run sync script
        env:
          X_API_KEY: ${{ secrets.X_API_KEY }}
          X_API_SECRET: ${{ secrets.X_API_SECRET }}
          X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
          X_ACCESS_TOKEN_SECRET: ${{ secrets.X_ACCESS_TOKEN_SECRET }}
          RSS_FEED_URL: ${{ env.RSS_FEED_URL }}
          STATE_FILE_PATH: ${{ env.STATE_FILE_PATH }}
          REQUIRED_TAG: ${{ env.REQUIRED_TAG }}
          TWEET_FORMAT: ${{ env.TWEET_FORMAT }}
        run: python sync_to_x.py

      - name: Configure Git User
        run: |
          git config user.name "${{ env.COMMIT_USER_NAME }}"
          git config user.email "${{ env.COMMIT_USER_EMAIL }}"

      - name: Commit and push state file (if changed)
        run: |
          git add ${{ env.STATE_FILE_PATH }} || echo "state file not found"
          if git diff --staged --quiet; then
            echo "No changes in state file."
          else
            git commit -m "${{ env.COMMIT_MESSAGE }}"
            git push origin main
          fi
